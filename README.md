# PitLane

Portal web (React) y app móvil (React Native) para gestionar el mantenimiento de flotas.

## Estructura

- `apps/web`: portal del gestor de flota
- `apps/mobile`: app del conductor (próximamente)
- `packages/core`: tipos, validaciones y cliente de API compartidos
- `api`: API REST simulada con json-server
